{
  "name": "shopify-data-ingestion-app",
  "version": "1.0.0",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",

   
    "build:frontend": "cd Frontend && npm install && npm run build && cd .. && rm -rf dist && cp -r Frontend/dist ./dist",

    
    "build": "npm run build:frontend",

   
    "postinstall": "npm run build"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "firebase-admin": "^13.5.0",
    "pg": "^8.11.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
